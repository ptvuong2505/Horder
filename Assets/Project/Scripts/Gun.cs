using UnityEngine;

/// <summary>
/// Gun.cs – Không còn dùng logic bắn trực tiếp.
/// Script này chỉ là MonoBehaviour placeholder cho prefab Gun cũ.
/// Logic bắn đã chuyển hoàn toàn sang AutoGun + WeaponManager.
/// Có thể xóa hoặc giữ lại nếu cần tham chiếu từ Inspector cũ.
/// </summary>
public class Gun : MonoBehaviour
{
    // Giữ trống - logic bắn đã chuyển sang AutoGun + WeaponManager
}
